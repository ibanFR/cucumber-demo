# Used by Maven to configure the JUnit Platform when running tests via Maven.

# Enable/Disable Cucumber Reports Service
cucumber.publish.enabled=false

# Select the Cucumber Object Factory
cucumber.object-factory = io.cucumber.jakarta.cdi.CdiJakartaFactory

# Disable scenarios with @wip tag
cucumber.filter.tags=not @wip
